# riftcode-cli

Core CLI scaffold for a local-first agent workflow.

## Commands

- `init`
- `status`
- `doctor`
- `sessions`
- `run <prompt...>`
- `resume [session-id]`
- `config show|get|set`

## Build

```bash
cargo build
```

